## Resubmission

This is a resubmission. In this version:

* Added the `[cph]` role to Authors@R in DESCRIPTION.
* Updated license copyright holders to use the full name of the author instead of the package name.
* Corrected external badge URLs in README to use HTTPS.
* Ignored hidden agent skill directories (`.agents`) in `.Rbuildignore` to avoid notes.
* Added strict input validation and regression tests for fuzzy joins.
* Corrected distance reporting for multi-column joins.

## R CMD check results

0 errors | 0 warnings | 0 notes

The local Windows check used R 4.6.1 with vignettes and examples enabled.
